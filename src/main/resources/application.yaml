server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://${postgres.railway.internal}:${5432}/${railway}
    username: ${postgres}
    password: ${muNkccGyVPZGpinjdJENDZyoiWXWqKdR}
    driver-class-name: org.postgresql.Driver
  sql:
    init:
      mode: always
      schema-locations: classpath:schema-postgresql.sql
